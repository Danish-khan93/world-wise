import Map from "../component/Map";
import SideBar from "../component/SideBar";

const AppLayOut = () => {
  return (
    <div className="flex ">
      <SideBar />
      <Map />

    </div>
  );
};

export default AppLayOut;
